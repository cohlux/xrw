from distutils.core import setup
from setuptools import setup, find_packages
setup(
    name='WPG',
    version='0.0.1',
    description='WaveProperGator is an interactive simulation framework for coherent X-ray wavefront propagation.',
    author='A. Buzmakov',
    packages=find_packages()
    );

